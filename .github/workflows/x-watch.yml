name: X Watch DiscordNEW8r

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: x-watch
  cancel-in-progress: false

jobs:
  watch:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: nm-${{ runner.os }}-node22-${{ hashFiles('package.json') }}
          restore-keys: |
            nm-${{ runner.os }}-node22-

      - name: Install deps
        run: |
          if [ ! -d node_modules/node-fetch ]; then
            npm install --no-audit --no-fund --no-package-lock
          fi
          rm -f package-lock.json

      - name: Watch @DiscordNEW8r
        env:
          X_WEBHOOK_URL: ${{ secrets.X_NEWS_WEBHOOK_URL }}
          DISCORD_X_WEBHOOK_URL: ${{ secrets.X_NEWS_WEBHOOK_URL }}
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          X_USERNAME: DiscordNEW8r
        # continue-on-error: sources publiques X souvent down
        continue-on-error: true
        run: node src/x_watch.js

      - name: Save seen ids
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          rm -f package-lock.json
          git add -f data/seen_x_posts.json data/x_watch_seen.json 2>/dev/null || true
          if git diff --staged --quiet; then
            echo "No seen changes"
            exit 0
          fi
          git commit -m "📌 x-watch seen $(date -u +%Y-%m-%dT%H:%MZ)" || true
          for i in 1 2 3 4 5; do
            if git pull --rebase --autostash origin main && git push origin HEAD:main; then
              exit 0
            fi
            git stash push -u -m "noise-$i" || true
            sleep 2
          done
          exit 0
